{
  "name": "demo",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "build": "npm run build:fast && npm run build:slow",
    "build:fast": "steal-tools build --main app-fast",
    "build:slow": "steal-tools build --main app-slow",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "can-connect": "^1.3.6",
    "can-connect-ndjson": "0.0.0",
    "can-define": "^1.0.16",
    "steal": "^1.2.7",
    "steal-stache": "^3.0.5",
    "steal-tools": "^1.1.2"
  },
  "steal": {
    "plugins": [
      "steal-stache"
    ]
  }
}
